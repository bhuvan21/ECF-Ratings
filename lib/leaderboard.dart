import 'package:flutter/material.dart';

// Yet to be done
class Leaderboard extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return new Container(
        child: new Center(
          child: new Icon(Icons.edit, size:150.0, color:Colors.blueAccent),
        )
    );
  }
}